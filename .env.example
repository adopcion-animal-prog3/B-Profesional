POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_strong_password_here
POSTGRES_DB=adopcion_db
POSTGRES_PORT=5432
# Optional: override PGDATA if needed by the image
# PGDATA=/var/lib/postgresql/data/pgdata

# NOTE: Do NOT commit real secrets. Copy this file to `.env` and
# fill with real credentials before running `docker compose up`.
